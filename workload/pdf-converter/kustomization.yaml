apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - helmrelease.yaml

commonLabels:
  app.kubernetes.io/name: pdf-converter
  app.kubernetes.io/version: v1.0
